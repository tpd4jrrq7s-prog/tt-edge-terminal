"""Phase 4 historical ingestion platform: provider-agnostic acquisition and import
of real historical table-tennis data, feeding the existing Phase 3 repositories.

See `historical_ingestion.service.HistoricalImportService` for the single
orchestrated entrypoint, `historical_ingestion.models` for every typed
contract, and `providers` for the provider-adapter layer.
"""
