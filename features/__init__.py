"""Phase 3 feature-engineering layer: leakage-safe rolling player and
matchup features built from temporal historical repositories.

See `features.builder.HistoricalFeatureBuilder` for the single
orchestrated entrypoint, and `features.models` for every typed contract.
"""
